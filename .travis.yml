language: csharp
dotnet: 2.2.301
install:
  - make install-deps
jobs:
  include:
    - stage: 'Tests'
      name: 'All Tests'
      script:
        - dotnet restore
        - make release-distro
        # - make test
    - stage: deploy
      name: 'Deploy to Github'
      script: skip

notifications:
  email: false
# before_deploy:
#   - make release-distro
# deploy:
#   provider: releases
#   on:
#     tags: true
#     repo: disunity-hq/disunity
#   draft: true
#   skip_cleanup: true
#   name: $TRAVIS_TAG
